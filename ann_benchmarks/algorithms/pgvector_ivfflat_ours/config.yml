float:
  any:
  - base_args: ['@metric']
    constructor: PGVectorIVFFlatMulti
    disabled: false
    docker_tag: ann-benchmarks-pgvector_ivfflat_multi_ours
    module: ann_benchmarks.algorithms.pgvector_ivfflat_multi_ours
    name: pgvector_ivfflat_multi_ours
    run_groups:
      # 固定配置：lists=100, workers=20, gpu=true
      lists-100-workers-20-gpu:
        arg_groups: [{lists: 100, num_workers: 20, use_gpu: true, batch_size: 5000}]
        args: {}
        query_args: [[1, 5, 10, 20, 40]]
